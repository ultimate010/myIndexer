SET(EXECUTABLE_OUTPUT_PATH ${PROJECT_BINARY_DIR}/bin)

ADD_EXECUTABLE(${PROJECT_NAME} my_indexer.cpp)
TARGET_LINK_LIBRARIES(${PROJECT_NAME} ${Boost_LIBRARIES} leveldb)  

INSTALL(TARGETS ${PROJECT_NAME} DESTINATION bin)
